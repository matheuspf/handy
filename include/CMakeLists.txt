if(NOT INCLUDE_INSTALL_DIR)
    set(INCLUDE_INSTALL_DIR ${CMAKE_BINARY_DIR}/Handy)
endif()

set(CMAKE_INSTALL_PREFIX ${INCLUDE_INSTALL_DIR})

install(DIRECTORY include
    DESTINATION ${INCLUDE_INSTALL_DIR}/Handy
)